<div class="alert-message block-message success">
  <p>
    <%= fb_profile_pic(request.target.challenger) %> <%= request.target.challenger.full_name %> bets you <%= number_to_currency(request.target.wager_amount, precision: 0) %> in Pickoff Cash that <%= request.target.challenger_entry.winner.name %> will beat <%= request.target.challenger_entry.loser.name %> on <%= request.target.game.starts_at.strftime('%B %e') %>.
  </p>
  <div class="alert-actions">
    <a class="btn small accept" href="#">Accept</a> <a class="btn small decline" href="#">Decline</a>
  </div>
</div>

